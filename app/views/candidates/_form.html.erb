<%= form_with(model: candidate, local: true) do |form| %>
  <% if candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
      <% candidate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :candidate_name %>
  </div>

  <div class="field">
    <%= form.label :sex %>
    <%= form.text_field :sex, id: :candidate_sex %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :candidate_phone %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :candidate_address %>
  </div>

  <div class="field">
    <%= form.label :formatted_address %>
    <%= form.text_field :formatted_address, id: :candidate_formatted_address %>
  </div>

  <div class="field">
    <%= form.label :birth_date %>
    <%= form.text_field :birth_date, id: :candidate_birth_date %>
  </div>

  <div class="field">
    <%= form.label :previous_experience %>
    <%= form.text_area :previous_experience, id: :candidate_previous_experience %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
